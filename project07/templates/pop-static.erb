// pop value on stack to static [<%= index %>]
@SP
M=M-1
A=M
D=M
@13
M=A
M=D // popped value off & storing @ temp (not TEMP)
@<%= @base %>.<%= index %> //@16
D=M
@<%= index %>
D=D+A // go to static[<%= index %>]
@14
M=D // store new index in temp
@13 // get temp and store in static[<%= index %>]
D=M
@14
A=M
M=D

