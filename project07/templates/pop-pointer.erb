// pop value on stack to pointer [<%= index %>]
// pointer = this + <%= index %>
@SP
M=M-1
A=M
D=M
@13
M=A
M=D // popped value off & storing @ temp (not TEMP)
@THIS
D=A
@<%= index %>
D=D+A // go to pointer[<%= index %>]
@14
M=D // store new index in temp
@13 // get temp and store in local
D=M
@14
A=M
M=D

